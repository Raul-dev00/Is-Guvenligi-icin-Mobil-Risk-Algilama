name: isg_risk_mobile
description: İş Sağlığı ve Güvenliği Mobil Risk Algılama
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  http: ^1.2.2
  sensors_plus: ^6.1.1
  geolocator: ^13.0.2
  connectivity_plus: ^6.1.1
  permission_handler: ^11.3.1
  flutter_secure_storage: ^9.2.4
  noise_meter: ^5.0.2
  intl: ^0.19.0
  flutter_background_service: ^5.1.0
  flutter_local_notifications: ^18.0.1
  shared_preferences: ^2.3.4

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
